a = float(input('Enter the value to be divided'))
z = float(input('Enter the value to divide'))

value = a % z #this operator is used to print the remainder value from a and z

print(f'The remainder of the values that you provided is {value}') #we are using f string to pull the current value mention in the program.