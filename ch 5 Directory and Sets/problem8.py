s = {8, 7, 12, "Harry", [1,2]} 
print(s)

#This is the wrong code, because the values inside sets are immutable and the values inside the list are mutable which 
#is not possible to exist together. Hence it gives the error that this is immutable.
